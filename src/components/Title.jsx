export const Title = ({width}) => (
    <g transform={`translate(${width / 3.5}, -20)`}>
        <text style={{
            fontSize: "2rem"
        }}>
            The Iris Flower
        </text>
    </g>
)
